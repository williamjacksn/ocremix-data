[project]
name = "ocremix-data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "htpy>=26.5.1",
    "lxml>=6.1.1",
]

[dependency-groups]
dev = [
    "ruff>=0.16.3",
]

[tool.ruff.lint]
extend-select = ["ANN", "Q", "RSE", "S"]
